namespace TechnoDating.Resources;

// Marker class used as the generic parameter of IStringLocalizer<Strings>.
// Translations live next to this file in Strings.resx (default English) and
// Strings.{culture}.resx satellite files (e.g. Strings.nl.resx for Dutch).
// Add a new language = add a new Strings.{code}.resx — no other code changes.
public class Strings { }
